# Choose an Environment

> Reached when `STAGE` is set to something other than `production`, `dev`, or `staging`.

Run command:

```shell
echo "Unknown STAGE — set it to production, dev, or staging"
```

---

_Authored for compatibility with Flik `v0.1`._
